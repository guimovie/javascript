/*
const nota1 = 10
const nota2 = 6.5
const nota3 = 7.5
const nota4 = 8
*/

//const media = (nota1 + nota2 + nota3 + nota4) / 4

const nota = [10, 6.5, 7.5, 8]
const media = (nota[0] + nota[1] + nota[2] + nota[3]) / nota.length
console.log(`A média das notas é ${media}`)